# Backend Tests

This directory contains backend API tests for the FastAPI application.

## Run all tests

From the project root:

```bash
pytest -q
```

## Run only backend tests

From the project root:

```bash
pytest -q tests/
```

## Run a single test file

From the project root:

```bash
pytest -q tests/test_app.py
```
